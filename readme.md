# demo data service with cloud foundry terraform provider
```shell
terraform init
# get a passcode from https://login.system.01.cf.eu01.stackit.cloud/passcode
export CF_SSO_PASSCODE=redacted
terraform apply
```